# User media — dropped into the system folders locally, never committed.
/scripture-game/*
/movies/*
/scripture/*
/music/*
/books/*
/games/*
# keep the folders themselves + their guide file tracked
!*/.gitkeep
!*/README.txt

# Downloaded full apps (REEL, ARCADE, …) — fetched by get-apps.js, never committed
/apps/

# Node / OS cruft
node_modules/
.DS_Store
Thumbs.db
*.log
